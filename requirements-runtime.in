# Exact direct runtime dependencies.
#
# These versions define the model-artifact ABI and the supported serving
# environment. Regenerate the fully transitive, hashed lock with:
#
#   make lock
#
# Do not relax the numpy/scikit-learn/joblib pins without rebuilding every
# serialised model artifact and re-running the compatibility checks.

# Core numerical / data stack
numpy==2.4.6
pandas==2.3.3
scipy==1.18.0
pyarrow==24.0.0

# Machine learning
scikit-learn==1.9.0
joblib==1.5.3

# Explainability
shap==0.52.0
numba==0.66.0
llvmlite==0.48.0

# Configuration and validation
PyYAML==6.0.3
pydantic==2.13.4
python-dotenv==1.2.2

# Visualisation
matplotlib==3.11.1
plotly==6.9.0

# API
fastapi==0.141.0
uvicorn[standard]==0.52.0

# Dashboard
streamlit==1.60.0
watchdog==6.0.0
